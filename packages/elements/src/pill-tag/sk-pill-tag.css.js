// GENERATED by scripts/build-elements-css.mjs — DO NOT EDIT.
// Source of record: packages/styles/src/pill-tag/sk-pill-tag.css
// Regenerate: node scripts/build-elements-css.mjs
const sheet = new CSSStyleSheet();
sheet.replaceSync("/* @spec-kitty/styles — sk-pill-tag + sk-pill-tag--eyebrow styles — tokens only, no hardcoded values */\n\n/* ---- Base tag ---- */\n/* A custom element defaults to `display: inline`; `inline-flex` matches the static form's own\n   display, which is what the two consumption paths must agree on (#77, #78). Inert in a\n   document, where the root IS the .sk-pill-tag span. */\n:host {\n  display: inline-flex;\n}\n\n.sk-pill-tag {\n  display: inline-flex;\n  align-items: center;\n  padding: var(--sk-space-1) var(--sk-space-3);\n  background: var(--sk-bg-pill);\n  border-radius: var(--sk-radius-pill);\n  font-family: var(--sk-font-sans);\n  font-size: var(--sk-text-xs);\n  font-weight: var(--sk-weight-medium);\n  color: var(--sk-fg-muted);\n  white-space: nowrap;\n  line-height: 1.5;\n}\n\n/* ---- Colour variants ---- */\n/* Using colour channels approach for broad compatibility.\n   Fallback opaque colours are provided for environments that do not\n   support CSS relative colour syntax (rgba(from var(...) r g b / α)). */\n\n.sk-pill-tag--green {\n  /* Fallback for older environments */\n  background: var(--sk-surface-tint-mint);\n  color: var(--sk-color-green);\n}\n\n.sk-pill-tag--purple {\n  background: var(--sk-surface-tint-lilac);\n  color: var(--sk-color-purple);\n}\n\n.sk-pill-tag--breaking {\n  background: var(--sk-surface-tint-sky);\n  color: var(--sk-color-red);\n}\n\n.sk-pill-tag--yellow {\n  background: var(--sk-surface-tint-butter);\n  color: var(--sk-color-yellow);\n}\n\n/* ---- Eyebrow: a SHAPE/SIZE modifier, not a second component ----\n   `.sk-eyebrow-pill` was a standalone class in this same file that restated the base\n   rule almost verbatim — same display, background, font family, weight, colour and\n   line-height — differing only in padding, corner radius and font size. #79 folded it\n   into `.sk-pill-tag--eyebrow` under the #139 ruling, so this file now describes one\n   component with two axes rather than two components sharing a directory.\n   Only the overrides remain: everything else comes from the base, which is what makes\n   it compose with the colour variants instead of competing with them. */\n.sk-pill-tag--eyebrow {\n  padding: var(--sk-space-2) var(--sk-space-4);\n  border-radius: var(--sk-radius-sm);\n  font-size: var(--sk-text-sm);\n}\n");
export default sheet;
