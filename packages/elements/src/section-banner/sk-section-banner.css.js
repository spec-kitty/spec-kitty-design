// GENERATED by scripts/build-elements-css.mjs — DO NOT EDIT.
// Source of record: packages/styles/src/section-banner/sk-section-banner.css
// Regenerate: node scripts/build-elements-css.mjs
const sheet = new CSSStyleSheet();
sheet.replaceSync("/* A custom element's UA default is `display: inline`, so without this the host cannot be sized\n   or spaced by a consumer and the inner flex row governs nothing outside itself. `inline-flex`\n   rather than `block` because a section banner is a badge that sits inline with surrounding\n   content — apps/demo/blog-demo.html already overrides the static form to `inline-flex` by\n   hand, which is the consumer telling us what the default should have been.\n   Inert in the static consumption path, where the root IS the .sk-section-banner div. */\n:host {\n  display: inline-flex;\n}\n\n.sk-section-banner {\n  display: flex;\n  align-items: center;\n  gap: var(--sk-space-3);\n  padding: var(--sk-space-2) var(--sk-space-4);\n  border-radius: var(--sk-radius-sm);\n  font-family: var(--sk-font-mono);\n  font-size: var(--sk-text-sm);\n  font-weight: var(--sk-weight-bold);\n  letter-spacing: 0.06em;\n  text-transform: uppercase;\n}\n\n.sk-section-banner--neutral {\n  background: var(--sk-surface-card);\n  color: var(--sk-fg-default);\n}\n\n/* --sk-surface-tint-lilac / --sk-on-tint-lilac, NOT --sk-surface-purple-tint.\n   The names this file used were defined nowhere in @spec-kitty/tokens, so `background:\n   var(--sk-surface-purple-tint)` was invalid at computed-value time and these two banners have\n   been rendering with NO background at all since they were written. Nothing caught it: the\n   hygiene gate that names undefined tokens runs on ADOPTED sheets, and section-banner had no\n   element until #77.\n   The tint family is the one this component was described in terms of — tokens.css:46 says\n   \"dark: v2.x banner / purple\" and :44 says \"numbers banner / green\", so the intended tokens\n   existed the whole time under the family's naming.\n   The foreground moves to the on-tint ink rather than --sk-color-purple: the colour tokens are\n   tuned for the dark page surface, and the light-mode tint surfaces are pastel, so the raw\n   colour on them is the low-contrast pairing --sk-on-tint-* exists to prevent. */\n.sk-section-banner--purple {\n  background: var(--sk-surface-tint-lilac);\n  color: var(--sk-on-tint-lilac);\n}\n\n.sk-section-banner--green {\n  background: var(--sk-surface-tint-mint);\n  color: var(--sk-on-tint-mint);\n}\n\n.sk-section-banner__dot {\n  font-size: var(--sk-text-base);\n}\n");
export default sheet;
