name: Docs Diagrams (drift check)

on:
  pull_request:
    paths: ["docs/architecture/assets/**"]
  push:
    branches: [main]
    paths: ["docs/architecture/assets/**"]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # actions/checkout v4.3.1
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # actions/setup-node v6.4.0
        with:
          node-version: "22"
          cache: npm
      - run: npm ci --ignore-scripts
      # O7: the rendering browser is pinned, not apt-provided. mermaid renders
      # text metrics differently between Chromium builds, so an unpinned
      # browser made this check non-reproducible — it reported drift on 8 of 8
      # committed SVGs with no .mmd modified. Playwright's Chromium is pinned
      # by the exact @playwright/test version in package.json, so this runner
      # and every contributor resolve the identical build.
      - name: Install pinned Chromium (Playwright)
        run: npx playwright install --with-deps chromium
      - name: "[ENFORCED] Render diagrams — drift check (FR-014, FR-015)"
        run: node scripts/render-diagrams.js --check
