{
  "name": "react-consumer-fixture",
  "projectType": "library",
  "sourceRoot": "fixtures/react-consumer/src",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p fixtures/react-consumer/tsconfig.json --noEmit"
      },
      "inputs": [
        "{projectRoot}/**/*",
        "{workspaceRoot}/tsconfig.base.json"
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "fixtures/react-consumer/src/**/*.tsx"
        ]
      }
    }
  },
  "tags": [
    "scope:fixture",
    "type:app"
  ]
}
