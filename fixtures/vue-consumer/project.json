{
  "name": "vue-consumer-fixture",
  "projectType": "library",
  "sourceRoot": "fixtures/vue-consumer/src",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p fixtures/vue-consumer/tsconfig.json --noEmit"
      },
      "inputs": ["{projectRoot}/**/*", "{workspaceRoot}/tsconfig.base.json"]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": { "lintFilePatterns": ["fixtures/vue-consumer/src/**/*.ts"] }
    }
  },
  "tags": ["scope:fixture", "type:app"]
}
