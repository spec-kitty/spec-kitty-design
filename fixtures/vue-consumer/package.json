{
  "name": "@spec-kitty/vue-consumer-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ADR-8 confirmation #4 (#81). Proves what a Vue 3 consumer actually needs to use the elements — measured in a real Vue render, not asserted. Vue lives here and only here, mirroring NFR-003's rule for React.",
  "devDependencies": {
    "@spec-kitty/elements": "*",
    "vue": "3.5.42"
  }
}
