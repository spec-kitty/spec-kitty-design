# Tracer: approach

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-08 · codex-orchestrator · Exact-head pre-merge Codex squad cycle 1 rejected 45dd657dcb7087b992601f7304e947570054a0ef. Four complete independent profiles converged on missing nine Ubuntu-authoritative visual baselines; three found nonconforming fixed-window zoom evidence; three found stale/incomplete acceptance evidence. Architect additionally found missing deterministic negative-path tests (non-callable writeText, focus/selection verification, range/selection exceptions) and redundant reduced-motion CSS. Remediation required before acceptance; reports: /tmp/issue257-final-review-{architect,debugger,reviewer,reducer}.md.

2026-09-08 · codex-orchestrator · Exact-head pre-merge Codex squad cycle 2 rejected 928ed8378466d045cb2d43809fa14df8feef19c6. Debugger approved; architect found viewport-coupled 20rem reflow fails a narrow host in a wide page; reviewer found missing live accessibility-tree and real Tab-order proof; reducer found plan/tasks/trace drift around the discovered 195px threshold and inert flex:none. Required remediation: inline-size container query plus wide-page/narrow-host Chromium/Firefox regression and SC-017 binding; before/after accessibility snapshots and real sentinel Tab paths including empty zero-stop; governance alignment; remove inert declaration. Reports: /tmp/issue257-review-cycle2-{architect,debugger,reviewer,reducer}.md.

2026-09-09 · codex-implementer · Cycle-2 implementation finding: the pre-fix 195px case reproduced a 2px value overflow, while viewport-only narrowing left the same defect untested for a roughly 115px host on a wide page. FR-017/SC-004 and the approved plan already require container-safe behavior at constrained available width, so replacing the page breakpoint with a host inline-size container query and binding that path to SC-017 is remediation within the existing WP, not a new layout feature.

2026-09-09 · codex-orchestrator · Clean Chromium and Firefox diagnostics showed that inline-size containment removes the host's intrinsic contribution inside Storybook's centered shrink-to-fit root: the percentage-sized story frame fell to its 48px padding and the host to 0px, while an explicit 115px host reflowed and contained correctly in both engines. Setting `inline-size: 100%` on the host remained 0px. The story-only frame now uses its intended maximum width bounded by the viewport minus Storybook's 2rem root padding, breaking the percentage/intrinsic cycle without imposing a component width. Post-fix C/F metrics were root 704px, frame 672px, and host/client/scroll/field 624px; focused C/F passed 30/30.

2026-09-09 · codex-orchestrator · The first complete post-rebase mutation pass reached 220/222 on e5202f4. Both copy-field SC-014 stylesheet-adoption arms also red the new SC-017 live container-layout test: removing all sheets or replacing the local sheet necessarily removes the `@container` rule. This is required cross-behavior evidence, so both arms now declare collateral and the mutation registry documents why guard 5 must observe it. The earlier host run was discarded after an external browser-provider race, and a first container retry was discarded because it overrode `HOME`; neither incomplete run is acceptance evidence.

2026-09-09 · codex-orchestrator · The first exact 3752ef2 real-UI replay exposed 4px of Chromium page overflow at 400%: the component and value were contained, but the story frame's `100vw` width included the zoomed vertical scrollbar. A definite-width/percentage-max frame alone still resolved against Storybook's centered shrink-to-fit root and failed at 195px in both engines. The story now opts into Storybook's fullscreen containing block and uses its definite intended width capped at `100%`; the deterministic 195px regression forces a vertical scrollbar and checks frame, host, value, and document containment. Focused C/F metrics passed 6/6, and genuine fixed-window UI zoom passed at 200%/400% in Chromium 151 and Firefox 153 with zero horizontal document overflow.
2026-09-09 · codex-orchestrator · Final exact-head cycle 3 rejected the post-#287 candidate on seven concrete gaps: containment removed the host's intrinsic contribution in flex/grid layouts; Storybook stubbed the clipboard on the wrong object and omitted meaningful labels; pointer-driven manual fallback did not guarantee a focus indicator; the four real clipboard-negative routes lacked C/F proof; the query used physical width instead of logical inline size; and data-outcome exposed unused transient state. The correction keeps one WP and one public element: full available-inline-size host sizing, logical container proof including vertical writing, direct/restored navigator stubs, meaningful story labels, real fallback paths, unconditional value focus styling, and no outcome attribute.
