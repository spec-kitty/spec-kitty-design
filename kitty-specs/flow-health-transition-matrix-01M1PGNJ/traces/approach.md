# Tracer: approach

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-05 · codex · Post-rebase dependency exception: train/elements-first commit 64ec1c7 introduced scripts/check-release-graph.mjs after WP02's authored-file boundary was approved. CI run 33938053543 requires every packages/styles/src component directory to have a matching packages/styles/package.json subpath export and requires the root changelog's element count/list to include sk-transition-matrix. This mission therefore permits only packages/styles/package.json plus root CHANGELOG.md as release-integration changes; all other manifests, lockfiles, tokens, ADRs, and sibling authored component source remain forbidden.

2026-09-05 · codex · Post-rebase CI visual audit found the narrow 390x844 maximum-scroll group heading clipped to “& RECOVERY” even though the DOM retained “Exceptions & recovery”; a rendered-text-range Playwright assertion measured text left -70.609375 outside viewport left 1. Commit 493d2dcf wraps the private group text and clamps it within the scroller using token-backed sticky insets while preserving desktop centering and the prior seven-owner rest/selected/hovered/pressed hit ownership. Chromium and Firefox passed; mutating only group-label position sticky to static reproduced the exact clipping red, then apply_patch restoration returned byte-identical source/generated/test hashes. The local diagnostic measured text 25..176.203125 inside viewport 1..389 at scrollLeft 380 and was removed without creating or blessing a baseline.

2026-09-05 · codex · Pass-1 gate remediation RED/GREEN: before production changes, the new real-browser element fixture failed exactly two press-lifecycle regressions: disabling after pointerdown retained data-pressed (expected false, received true), and removing then reintroducing a keyboard-pressed route resurrected data-pressed (expected false, received true); after clearing private press state before render when non-selectable or when its route is absent, the focused fixture passed 35/35. For the non-registry five-tone theme acceptance behavior, an apply_patch source swap removed only the backward route from the shared Default/LightMode fixture; the Chromium Storybook contract failed with expected [forward, completed, blocked, recovery, backward] but received [forward, completed, blocked, recovery], then exact restoration passed. For the non-registry runtime tone guard, an apply_patch source swap weakened isTone from TONES membership to string-only; the focused unknown-runtime-tone case failed expected empty-state copy 'No transition data' but received empty text, then exact restoration passed. These acceptance behaviors deliberately remain outside mutations.json because that harness is restricted to ADR-11 SC id/subject pairs; their committed runtime/browser tests are the durable guards.

2026-09-05 · codex · Pass-2 gate remediation RED/GREEN: the empty-column-id fixture now supplies one blank-id column and a matching blank-key value map. In an isolated in-mission worktree, removing only `columnIds.some((id) => !validId(id))` made `npx vitest run --project browser fixtures/elements-behaviour/src/sk-transition-matrix.test.ts --reporter=default -t "empty column id"` fail its named case 1/1 because the expected `No transition data` state was absent; restoration passed 1/1. For exact tone-to-token proof, changed only recovery CSS from `var(--sk-color-purple)` to `var(--sk-color-red)`, regenerated CSS and Storybook, and ran `npx playwright test apps/storybook/src/tests/sk-transition-matrix.spec.ts --project=chromium --grep "Default dark and LightMode"`; it failed expected `rgb(184, 169, 224)`, received `rgb(233, 115, 115)`. The distinct-colour assertion now follows the exact per-tone token assertions, so the intended mapping mismatch is the first red. Restored authored and generated CSS and the same test passed 1/1; restored hashes are authored CSS `93fd8b2b59a5b852f09f7bbbfa97e954a57912ebac5413b7dbd17eaba378c1f9` and element source `38813bea628a182fa0df253ec9dfcee3be4bdd205831fe0f49143994530c6d5f`. For replacement-total derivation, changed only the route reduce arm from `+ route.values[column.id]` to `+ route.values[column.id] + 1`; the focused `bar ratios use the current global maximum` behavior test failed expected route total `30`, received `34`, then restoration passed 1/1. The pass-2 invalid-matrix press regression independently failed 1/36 on the old route-presence-only cleanup, expected no `data-pressed` after restoration but received it; the `validateMatrix(columns, routes)` cleanup plus behavior and browser coverage restored the full suite to 228/228 and Chromium component coverage to 13/13.
