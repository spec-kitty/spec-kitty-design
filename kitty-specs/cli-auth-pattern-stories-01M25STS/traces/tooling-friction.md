# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-09-11 · post-merge-mission-review · Two security/accessibility fixes shipped initially protected by nothing: Story 2's Approve/Deny had no name/value differentiator (identical submits) and the consent scopes had no list semantics (orphan text instead of <ul>/<li>). Both landed in the pass-1 fold (83f0a8e9) but the existing test assertions (actionOrder projecting only {type, classes, text}; a descendant-only sk-pill-tag locator) could not detect a revert of either fix -- three lenses in the pass-2 squad independently found this, all 13 checks would stay green if either fix were reverted. Fixed in pass-2 fold (9d2cdd1e) by widening the assertion and adding a direct structural check, then red-verifying by reverting the source and confirming the new assertions fail. Lesson: a 'the fix landed and tests are green' review is not evidence the fix is protected -- ask whether reverting the fix specifically would fail something.

2026-09-11 · post-merge-mission-review · A lens (randy-reducer) withdrew two of its own pass-1 findings in the pass-2 squad round on PR #409, both recorded rather than buried. (1) A 'two redundant LightMode terminal stories' finding was wrong: it conflated 'one per render function' with 'one per story', but spec.md FR-005 requires a LightMode variant for every story, and renderTerminal serves four distinct stories -- all six terminal stories were kept. (2) A role="list" disagreement between two lenses was adjudicated against the source: the repo's existing role="list" precedent exists specifically to compensate for custom-element children (<sk-check-bullet>) that are not real list items; this mission's <ul>/<li> children are real list items, so no role was added. Lesson: an adversarial lens finding is a hypothesis, not a verdict -- withdrawal-with-cause against a named contract clause (here, FR-005) is a healthy outcome of the squad process, not a process failure, and this was the second consecutive pass in which that same lens caught its own error before it became a permanent record.

2026-09-11 · post-merge-mission-review · The mission-closing comment on issue #329 (2026-09-11T15:54:39Z) claims five findings were 'filed rather than folded': #417 and #418 are real, verifiable GitHub issues, but the absent NFR/C acceptance-matrix rows, the data-model.md 'no action group renders' inaccuracy, and the 200%-zoom media-query-not-firing finding were never actually opened as issues (verified by search: no issue after #419 references any of them). This repeats, on a smaller scale, the exact failure the squad's own pass-2 PR comment named and warned about one layer up ('a previous fold commit records seven deferrals as filed instead and they had not been filed... the orchestrator's error, not the mission's'). Lesson: 'filed, not folded' needs the issue number in the same sentence as the claim, or a follow-up automated check that every named-but-not-numbered deferral actually resolves to an open issue before the mission's closing comment is trusted.

2026-09-11 · post-merge-mission-review · General platform risk relevant to this mission's multi-dependency wait window (held stopped until #320, #321 and #303 all landed on train/elements-first, then rebased once): a PR that conflicts with its base gets NO CI run at all, because GitHub cannot compute a merge ref to run Actions against -- the checks list is simply empty rather than red. This is visually identical to a CI outage and easy to misdiagnose as 'CI is down' rather than 'this branch needs a rebase.' Not independently reproduced against PR #409/#320(#341)/#321(#339)/#303(#357)'s own recorded check history in this review -- recorded here as a known platform characteristic worth naming for any future mission holding itself stopped on multiple in-flight dependencies the way this one did.

2026-09-11 · post-merge-mission-review · acceptance-matrix.json ships 20 FR rows, overall_verdict pass, negative_invariants: [] -- confirmed as a TOOLING LIMITATION, not mission negligence, by reading the generator: mission_finalize.py's _scaffold_acceptance_matrix_if_lane_based only ever seeds criteria from functional_spec_requirement_ids (the FR-prefixed subset of parse_requirement_ids_from_spec_md's 'all' key); NFR-*/C-* ids are parsed into that 'all' key but never reach scaffold_acceptance_matrix's requirement_ids parameter, so no NFR/C row is ever auto-seeded for any software-dev mission. acceptance_verdict.py's --criterion mode requires the row to already exist ('Unknown criterion') with no path to add one -- hand-editing acceptance-matrix.json is the only way to add an NFR/C row, which this repo's own norms forbid. Separately, negative_invariants is an unrelated mechanism (bulk-edit/drift post-consolidation tracking, matrix.py's NI-1..NI-5) never intended to carry spec NFR/C evidence, so its being empty here is not itself a second gap. Net effect: overall_verdict: pass is correct per the tool's current, narrower definition (all 20 FR criteria passed), but that definition has no structural representation for NFR-001..009/C-001..C-012 at all -- 'pass' does not mean those were formally gated, only that the FR rows were. Filed by the pass-2 squad as a deferral, never opened as a GitHub issue (see the companion tooling-friction entry on unfiled 'filed, not folded' claims). Recommend a real spec-kitty issue: extend scaffold_acceptance_matrix / acceptance_verdict to seed and accept NFR-*/C-* criterion rows the same way FR-* rows work today.
